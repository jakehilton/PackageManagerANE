<?xml version="1.0"?>
<project name="package_manager_ane" default="jar" basedir=".">

    <property name="output.dir" value="jar"/>
    <property name="jar.filename" value="PackageManagerANE.jar"/>
    <property name="core.build.dir" value="bin"/>

    <target name="jar">
        <jar jarfile="${output.dir}/${jar.filename}">
            <fileset dir="${core.build.dir}"/>
        </jar>
    </target>
    
</project>
